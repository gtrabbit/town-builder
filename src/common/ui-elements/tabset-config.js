export default class TabsetConfig {
    constructor(tabConfigs, tabHeight, tabWidth) {
        this.tabHeight = tabHeight;
        this.tabWidth = tabWidth;
        this.tabConfigs = tabConfigs;
    }
}